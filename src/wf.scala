import PersimmonSyntax.*
import PersimmonTyping.*

object PersimmonWF {

    // Well-formedness of definitions
    def wfDef(K: PathCtx, d: Definition): Boolean = {
        true
    }

    // Well-formedness of types
    def wfType(K: PathCtx, t: Type): Boolean = {
        true
    }

}